version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: csd_study_mongodb
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: csd_study
    volumes:
      - mongodb_data:/data/db
    restart: unless-stopped

volumes:
  mongodb_data:
